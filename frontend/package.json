{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "http-server ./src -a localhost -p 8000 -c-1"
  },
  "dependencies": {
    "angular": "^1.7.9",
    "http-server": "^0.12.1"
  }
}
